package simple;

/*
    Write a function that takes in a non-empty array of distinct integers and an integer representing
    a target sum, the function should return them in array. If no two numbers sum to the target
    sum, the function should return an empty array. Assume that there will be at most one pair
    of numbers summing up to the target sum.

    Sample input: [3,5,-4,8,11,1,-1,6],10
    Sample output: [-1,11]
 */
public class TwoNumberSum {
    public int[] twoNumberSum(int[] array, int targetSum){
        int[] arr = {0,0};
        return arr;
    }

    public boolean search(int[] array, int searchValue, int index){
        return false;
    }
}
